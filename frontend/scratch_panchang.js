const { calculatePanchang } = require('panchang-ts');
// Wait, is it calculatePanchang? Let's check require('panchang-ts')
const panchang = require('panchang-ts');
console.log(Object.keys(panchang));
